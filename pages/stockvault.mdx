# Stoic

import { VaultLink } from '../components'
import vaultsData from '../data/vaults.json'

export const vault = vaultsData.vaults.stockvault

<VaultLink url={vault.url} name="StockVault" />

## Key Metrics

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(90px, 1fr))', gap: '0.5rem', margin: '0.75rem 0' }}>
  <div style={{ background: '#374151', padding: '0.625rem', borderRadius: '6px', textAlign: 'center' }}>
    <div style={{ fontSize: '1.125rem', fontWeight: 'bold', color: 'white' }}>{vault.metrics.stockCompanies}</div>
    <div style={{ fontSize: '0.625rem', color: '#9ca3af', marginTop: '0.25rem' }}>STOCK</div>
  </div>
  <div style={{ background: '#374151', padding: '0.625rem', borderRadius: '6px', textAlign: 'center' }}>
    <div style={{ fontSize: '1.125rem', fontWeight: 'bold', color: 'white' }}>{vault.metrics.investmentCompanies}</div>
    <div style={{ fontSize: '0.625rem', color: '#9ca3af', marginTop: '0.25rem' }}>INVESTMENTS</div>
  </div>
  <div style={{ background: '#374151', padding: '0.625rem', borderRadius: '6px', textAlign: 'center' }}>
    <div style={{ fontSize: '1.125rem', fontWeight: 'bold', color: 'white' }}>{vault.metrics.quarters}</div>
    <div style={{ fontSize: '0.625rem', color: '#9ca3af', marginTop: '0.25rem' }}>QUARTERS</div>
  </div>
</div>

## Stock Companies (Quarterly Financials)

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(100px, 1fr))', gap: '0.375rem', marginTop: '0.5rem' }}>
  {vault.stockCompanies.map((company) => (
    <div key={company} style={{ padding: '0.375rem 0.5rem', background: '#1f2937', borderLeft: '2px solid #6b7280', borderRadius: '0 4px 4px 0' }}>
      <span style={{ color: 'white', fontWeight: '500', fontSize: '0.7rem' }}>{company}</span>
    </div>
  ))}
</div>

## Investment Reports (Portfolio Companies)

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(100px, 1fr))', gap: '0.375rem', marginTop: '0.5rem' }}>
  {vault.investmentCompanies.map((company) => (
    <div key={company} style={{ padding: '0.375rem 0.5rem', background: '#1f2937', borderLeft: '2px solid #6b7280', borderRadius: '0 4px 4px 0' }}>
      <span style={{ color: 'white', fontWeight: '500', fontSize: '0.7rem' }}>{company}</span>
    </div>
  ))}
</div>

## Data Available

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(140px, 1fr))', gap: '0.5rem', marginTop: '0.75rem' }}>
  <div style={{ padding: '0.625rem', background: '#1f2937', borderRadius: '6px', borderTop: '2px solid #6b7280' }}>
    <div style={{ color: '#9ca3af', fontWeight: '600', marginBottom: '0.375rem', fontSize: '0.75rem' }}>Stock Companies</div>
    <ul style={{ color: 'white', marginLeft: '1rem', fontSize: '0.7rem' }}>
      <li>Quarterly financial reports</li>
      <li>Strategic analysis</li>
      <li>Key metrics comparison</li>
      <li>Multi-quarter trends</li>
    </ul>
  </div>
  <div style={{ padding: '0.625rem', background: '#1f2937', borderRadius: '6px', borderTop: '2px solid #6b7280' }}>
    <div style={{ color: '#9ca3af', fontWeight: '600', marginBottom: '0.375rem', fontSize: '0.75rem' }}>Investment Reports</div>
    <ul style={{ color: 'white', marginLeft: '1rem', fontSize: '0.7rem' }}>
      <li>Company overview</li>
      <li>Key milestones</li>
      <li>Product updates</li>
      <li>Investment signals</li>
    </ul>
  </div>
</div>

---

<div style={{ marginTop: '1rem' }}>
  <VaultLink url={vault.url} name="Open StockVault" />
</div>

<div style={{ marginTop: '0.5rem', color: '#9ca3af', fontSize: '0.625rem' }}>
  Last updated: January 30, 2026
</div>
